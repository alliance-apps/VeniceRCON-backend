get:
  summary: retrieves a list of users with their permissions
  description: |
    retrieves a list of users with their permissions
  tags:
    - "InstanceUser"
  security:
    - bearerAuth: ["INSTANCEUSER#ACCESS"]
  responses:
    '200':
      description: list of users
      content:
        application/json:
          schema:
            $ref: "../../../../schemes/InstanceUser.yaml#/InstanceUsers"